import styled from "styled-components";
import { colors } from "../../../colors";

const PerguntaSection = styled.section`
  width: 65rem;
  margin: 1rem auto 0 auto;
  padding: 1rem;
  border-radius: 2px;
  background: ${colors.primary};
`;

export { PerguntaSection };
